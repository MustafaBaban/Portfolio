name: Build and Deploy

on:
  push:
    branches:
      - main  # Replace 'main' with your default branch name if it's different

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: 16  # Replace with your desired Node.js version

    - name: Install dependencies
      run: npm ci

    - name: Build project
      run: npm run build  # Replace with your build command

    - name: Push build to build branch
      uses: EndBug/add-and-commit@v7
      with:
        branch: build
        message: "Update build"
        add: "dist/*"  # Replace with the path to your build output folder